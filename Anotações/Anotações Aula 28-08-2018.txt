Anotações Aula 28/08/2018

Usando a máquina da univali

*Primeiro -> fazer download do Git Portable
*Segundo  -> Extrair
*Terceiro -> Fazer Login no site do Git Hub
*Quarto   -> Se for a Primeira vez na Maquina especifica, usar "git clone http://...(Copiar do Site Git Hub)"
*Quinto   -> Se já tiver usado a máquina antes, usar "git pull" para atualizar o diretório

-> Git Bash
*cd -> (escolher diretório a ser utilizado, ou seja, para acessar o repositório)
*cd .. -> (volta uma hierarquia do diretório)
*ls -la -> (mostra tudo que há dentro)

-> Programando com o NetBeans
*System.out.println
->Classe abstrata
uma superclasse que não pode ser instanciada
só pai

->Classe final
é uma classe estéril, não pode ter filho
só filho

public final double c= 12345...			// public (pode ser acessado de qualquer lugar)
						//final (uma constante)
						//double (tipo)
						//c=12345... (dado da constante)

class Aluno
	public String Nome
	public Char[] CPF
	public static int Num_Alunos;

b. 			//não estão declarados como static
String. 		//estão declarados como static

ctrl + shift setpraAbaixo		//No NetBeans

Construtores ñ tem retorno

*is significa get em métodos booleanos

->Encapsulamento
ctrl + alt + shift + e 		-> (atalho no netBeans para fazer "get" e "set"

alt + shift + f 		-> (identa tudo)
